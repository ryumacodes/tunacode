# TunaCode Environment Variables
#
# Primary configuration is handled via ~/.config/tunacode.json
# These environment variables provide overrides and API credentials.
#
# Copy this file to .env and fill in the values you need.
# .env is already in .gitignore — never commit secrets.

# ─── API Keys ────────────────────────────────────────────────────────
# Provider API keys (set the one(s) you use)
ANTHROPIC_API_KEY=
OPENAI_API_KEY=
# GOOGLE_API_KEY=
# MISTRAL_API_KEY=
# GROQ_API_KEY=

# ─── Runtime Overrides ───────────────────────────────────────────────
# Override ripgrep binary location (default: system ripgrep or bundled)
# TUNACODE_RIPGREP_PATH=/usr/local/bin/rg

# Max parallel tool execution (default: cpu_count or 4)
# TUNACODE_MAX_PARALLEL=8

# XDG base directory for session/log storage (default: ~/.local/share)
# XDG_DATA_HOME=~/.local/share

# ─── Feature Flags ───────────────────────────────────────────────────
# Enable with: 1, true, yes, or on (case-insensitive)
# TUNACODE_FF_PARALLEL_TOOL_EXECUTION=1
# TUNACODE_FF_NEW_TOOL_RENDERER=1
